source 'https://rubygems.org'

gemspec

group :dev, :default do
  # runtime/dev dependencies
  gem 'lru_redux', '~>1.1.0'
end

group :dev do
  # dev dependencies
  gem 'rubocop', '~>0.41.1'
  gem 'rspec', '~>3.5.0'
  gem 'webmock', '~>2.1.0'
end
